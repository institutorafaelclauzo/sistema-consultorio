/// <reference types="vite/client" />

/** Hora do build, escrita pelo vite.config.ts. */
declare const __VERSAO__: string
/** Commit publicado, ou "local" quando o build roda fora do GitHub. */
declare const __COMMIT__: string
